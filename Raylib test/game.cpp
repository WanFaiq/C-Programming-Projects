#include "raylib.h"
int main()
{
    int width;
    width = 350;
    InitWindow (width, 200, "Scammer's corner");

    while (true )
    {
       BeginDrawing();
       ClearBackground(RED);
       EndDrawing();

    }
}